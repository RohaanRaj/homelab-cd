apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  name: data-pvc
  namespace: media

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: music
  resources:
    requests:
      storage: 1Gi
